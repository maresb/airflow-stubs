from airflow.decorators import task as task
from airflow.decorators.base import add_one as add_one, sum_it as sum_it
from airflow.models.dag import DAG as DAG, dag as dag
from airflow.models.xcom_arg import added_values as added_values
